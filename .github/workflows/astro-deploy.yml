name: Deploy Astro

on:
  push:
    branches: [main]
    paths:
      - 'astro/**'
      - '.github/workflows/astro-deploy.yml'
      - '.github/actions/astro-cloudflare-deploy/**'
  workflow_dispatch:

concurrency:
  group: astro-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: astro-production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy Astro worker to Cloudflare
        uses: ./.github/actions/astro-cloudflare-deploy
        with:
          mode: deploy
          cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          cloudflare-account-id: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
