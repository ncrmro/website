if command -v devenv >/dev/null 2>&1; then
  source <(devenv direnv-export)
elif [ -f flake.nix ]; then
  use flake
else
  echo "devenv not found and no flake.nix available; install devenv or restore flake.nix"
fi

dotenv_if_exists
